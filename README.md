# DeepLearning_CovType
This project explores neural networks (MLP) and ensemble models (Random Forest, XGBoost) on the UCI Forest CoverType dataset. It includes model tuning, evaluation metrics, and a comparative analysis of why tree-based methods outperform deep learning on structured data.
